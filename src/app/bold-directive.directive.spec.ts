import { BoldDirectiveDirective } from './bold-directive.directive';

describe('BoldDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
